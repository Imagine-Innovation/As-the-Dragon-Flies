# global codeception file to run tests from all apps
include:
    - common
    - frontend
    - backend
paths:
    output: tests/_output
    data: tests/_data
    support: tests/_support # Base structure
    envs: tests/_envs
settings:
    colors: true
    shuffle: false
    lint: true