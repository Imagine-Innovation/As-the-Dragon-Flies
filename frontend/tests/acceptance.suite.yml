namespace: frontend\tests   # Codeception uses this as the base prefix
actor: AcceptanceTester
modules:
    enabled:
        - WebDriver:
            url: http://localhost:8080
            browser: chrome
        - Yii2:
            part: init
